{{#section 'head'}}
<style>
    button {
            {
                {
                 !-- margin-top: 10px;
                --
            }
        }
    }

    .toolbar {
        display: flex;
        flex-direction: row;
    }

    .toolbar .input-wrapper {
        flex: 0.8;
    }

    .toolbar .add-wrapper {
        flex: 0.2;
    }

    .toolbar .add-wrapper button {
        margin-left: 10px;
    }
</style>
{{/section}}

{{> navbar showlogout = true}}

<div class="container">

    <h1>Your Todos</h1>

    <div class="toolbar">
        <div class="input-wrapper">
            <div class="input-group mb-3">
                <input id="task" type="text" name="task" placeholder="task" class="form-control" aria-label="username">
            </div>
        </div>

        <div class="add-wrapper">
            <button id="button" type="button" class="btn btn-primary">Add</button>
        </div>
    </div>

    <ul class="list-group">

        {{#each todos}}
        <li class="list-group-item">{{this}}</li>
        {{/each}}


    </ul>

</div>

{{#section 'scripts'}}
<script>

    const task = document.getElementById('task');

    document.getElementById('button').addEventListener("click", () => {
        console.log({ task: task.value });

        let headers = new Headers();

        headers.append('Accept', 'application/json');
        headers.append("Content-Type", 'application/json');

        fetch("/todos", {
            method: "POST",
            body: JSON.stringify({
               task: task.value
            }),
            headers
        }).then(() => window.location.reload())
    })




</script>
{{/section}}